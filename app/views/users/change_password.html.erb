<h1><%=t(:update_password, :scope => :topics)%></h1>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for @user, :url => {:action => 'update_password'}, :html => {:class => "form well"} do |u|%>
            <%= render 'common/error_messages', :object => u.object %>
            <%=u.label :password %>
            <%=u.password_field :password, class: 'form-control'%>
            <%=u.label :password_confirm %>
            <%=u.password_field :password_confirmation, class: 'form-control'%>
            <%=u.submit "Update", class: "btn btn-primary"%>
        <% end %>
    </div>
</div>
