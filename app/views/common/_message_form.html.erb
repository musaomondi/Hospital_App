<%= form_for([current_patient.messages.build], remote: true) do |f| %>
    <%= render 'common/error_messages', object: f.object %>
    <div class="field">
        <%= f.text_area :content, placeholder: "Compose new message..." %>
    </div>
    <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
